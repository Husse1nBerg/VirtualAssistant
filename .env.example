# ── Server ──────────────────────────────────────────
PORT=3000
NODE_ENV=development
BASE_URL=https://your-domain.ngrok-free.app   # public URL for Twilio webhooks

# ── Twilio ──────────────────────────────────────────
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_PHONE_NUMBER=+1XXXXXXXXXX              # your Twilio number
TWILIO_WHATSAPP_FROM=                         # for WhatsApp: sandbox number e.g. +14155238886 (see README); leave empty to use TWILIO_PHONE_NUMBER
TWILIO_WEBHOOK_SECRET=                         # optional: for webhook signature verification

# ── Your Personal Number (notification recipient) ───
OWNER_PHONE_NUMBER=+1XXXXXXXXXX               # your Rogers number / personal number
OWNER_WHATSAPP_NUMBER=whatsapp:+1XXXXXXXXXX   # WhatsApp-enabled number (optional)

# ── Anthropic (Claude) ─────────────────────────────
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# ── Deepgram (STT) ─────────────────────────────────
DEEPGRAM_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── TTS Provider ────────────────────────────────────
TTS_PROVIDER=deepgram                          # deepgram | elevenlabs
ELEVENLABS_API_KEY=                            # only if TTS_PROVIDER=elevenlabs
ELEVENLABS_VOICE_ID=                           # only if TTS_PROVIDER=elevenlabs

# ── Database ────────────────────────────────────────
DATABASE_URL=file:./dev.db                     # SQLite for dev; swap to postgres:// for prod

# ── Logging ─────────────────────────────────────────
LOG_LEVEL=info                                 # trace | debug | info | warn | error | fatal
